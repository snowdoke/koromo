# PowerShellで実行するコマンドまとめ

## 0. Node.js / npm のインストール

```powershell
winget install OpenJS.NodeJS
```

Node.js をインストールした後に vscode の再起動が必要です。

## 1. Node.js / npm のバージョン確認

Node.js と npm が使えるか確認する。

```powershell
node -v
npm -v
```

バージョンが表示されればOK。

---

## 2. PowerShellでnpmが実行できない場合

以下のようなエラーが出る場合がある。

```txt
npm : このシステムではスクリプトの実行が無効になっているため、
ファイル C:\Program Files\nodejs\npm.ps1 を読み込むことができません。
```

その場合は、現在のユーザーだけPowerShellスクリプトの実行を許可する。

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

確認が表示されたら、`Y` を入力して Enter。

その後、もう一度 npm を確認する。

```powershell
npm -v
```

一時的に現在のPowerShellだけ許可したい場合は、こちらを使う。

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
```

---

## 3. プロジェクト作成

SSG用の作業フォルダを作成する。

```powershell
mkdir my-ssg
cd my-ssg
```

npmプロジェクトを初期化する。

```powershell
npm init -y
```

---

## 4. package.json の name を現在のフォルダ名に合わせる

現在のフォルダ名を取得して、`package.json` の `name` に設定する。

```powershell
$name = Split-Path -Leaf (Get-Location)
npm pkg set "name=$name"
```

`package-lock.json` 側も更新する。

```powershell
npm install --package-lock-only
```

---

## 5. フォルダ名を小文字・ハイフン区切りにして name に設定する場合

npmのパッケージ名として扱いやすいように、フォルダ名を小文字化し、スペースをハイフンに変換して設定する。

```powershell
$name = (Split-Path -Leaf (Get-Location)).ToLower() -replace '\s+', '-'
npm pkg set "name=$name"
npm install --package-lock-only
```

例：

```txt
My SSG Tool
```

上記のフォルダ名は、以下のような `name` になる。

```txt
my-ssg-tool
```

---

## 6. チーム配布用の name を設定する場合

チーム共通ツールとして配布する場合は、スコープ付きパッケージ名にすることもできる。

```powershell
npm pkg set "name=@your-team/ssg"
npm install --package-lock-only
```

`@your-team/ssg` の部分は、実際のチーム名やプロジェクト名に合わせて変更する。

---

## 7. 誤ってnpmに公開しないようにする

npmに公開しない場合は、`private` を `true` にする。

```powershell
npm pkg set private=true
```

確認する。

```powershell
npm pkg get private
```

`true` が表示されればOK。

---

## 8. 開発用パッケージをインストールする

ファイル変更監視用の `chokidar-cli` を入れる。

```powershell
npm install -D chokidar-cli
```

SCSSをCSSに変換するための `sass` を入れる。

```powershell
npm install -D sass
```

出力されるHTML、CSS、JSを整形するための `prettier` を入れる。

```powershell
npm install -D prettier
```

front matterをYAML配列として書けるようにする `js-yaml` を入れる。

```powershell
npm install -D js-yaml
```

まとめて入れる場合はこちら。

```powershell
npm install -D chokidar-cli sass prettier js-yaml
```

---

## 9. build / watch コマンドを package.json に追加する

ビルド用コマンドを追加する。

```powershell
npm pkg set "scripts.build=node build.js"
```

保存時に自動ビルドする watch コマンドを追加する。

```powershell
npm pkg set "scripts.watch=chokidar `"src/**/*`" `"build.js`" -c `"npm run build`" --initial"
```

---

## 10. ビルドを実行する

HTML、CSS、JS、画像などを `dist` に出力する。

```powershell
npm run build
```

---

## 11. 保存時に自動ビルドする

開発中は以下を実行したままにする。

```powershell
npm run watch
```

`src/` 配下のファイルや `build.js` を保存すると、自動で `npm run build` が実行される。

---

## 12. Gitで node_modules を除外する

`.gitignore` を作成する。

```powershell
New-Item .gitignore -ItemType File
```

`.gitignore` に `node_modules/` を追加する。

```powershell
Add-Content .gitignore "node_modules/"
```

必要に応じて、ビルド成果物の `dist/` も除外する。

```powershell
Add-Content .gitignore "dist/"
```

---

## 13. すでに node_modules をGit管理してしまった場合

Gitの追跡対象から `node_modules` を外す。

```powershell
git rm -r --cached node_modules
```

`.gitignore` を追加してコミットする。

```powershell
git add .gitignore
git commit -m "Ignore node_modules"
```

---

## 14. 別の環境で依存パッケージを復元する

`node_modules/` はGit管理しないため、別のPCでは以下を実行して復元する。

```powershell
npm install
```

これで `package.json` と `package-lock.json` を元に、必要なパッケージが再インストールされる。
