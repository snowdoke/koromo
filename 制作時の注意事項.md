# マニュアル

## html、CSSの新規作成
htmlとCSSの作成時に、最低限必要なテンプレートをコピーペーストする
```
参照先：〇〇.txt
```

---

## SASSのWatchが常時ONになっている場合
srcフォルダのscssフォルダの中にcssファイルが作成されるので、以下の対策を推奨
・WatchをOFFにする
・自動生成されたcssファイルはプッシュしない

```
自動WatchをOFFにする方法
Ctrl+Shift+P   
→「基本設定：ユーザー設定を開く（JSON）」を選択
→ liveSassCompile.settings.watchOnLaunch　の値を「false」にする
```

---

## imgやCSS、JSの使用時
@img　@css　@js　の形で入力する

---

## Live Serverで下層ページを見る方法

VSCodeで **Go Live** を押すと、`dist` をルートとして開く。

基本URLは以下。

```
http://127.0.0.1:5500/
```

directory版の場合は、
```
http://127.0.0.1:5500/about/
```

file版の場合は、
```
http://127.0.0.1:5500/about.html
```